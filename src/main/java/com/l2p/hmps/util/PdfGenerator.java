package com.l2p.hmps.util;

import com.l2p.hmps.model.MedicalRecord;
import com.l2p.hmps.model.Prescription;
import com.l2p.hmps.model.User;

public class PdfGenerator {

    // Stub for MedicalRecord PDF
    public static byte[] generateMedicalRecordPdf(MedicalRecord record, User patient) {
        return new byte[0]; // temporary, replace with actual PDF logic
    }

    // Stub for Prescription PDF
    public static byte[] generatePrescriptionPdf(Prescription rx, User patient, User doctor) {
        return new byte[0]; // temporary, replace with actual PDF logic
    }
}