<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-departments" author="Dev2">

        <!-- ✅ CREATE TABLE -->
        <createTable tableName="departments">

            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="description" type="TEXT"/>

            <column name="head_doctor_id" type="UUID"/>

            <column name="active" type="BOOLEAN" defaultValueBoolean="true"/>

            <!-- ✅ FIX: ADD TIMESTAMPS -->
            <column name="created_at" type="TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP"/>

        </createTable>

        <!-- ✅ INDEXES (optional but good) -->
        <createIndex tableName="departments" indexName="idx_department_name">
            <column name="name"/>
        </createIndex>

        <createIndex tableName="departments" indexName="idx_department_head">
            <column name="head_doctor_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>